{
  "name": "rideon-monorepo",
  "version": "1.0.0",
  "description": "Complete Uber-like ride-hailing platform",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:admin\" \"npm run dev:web\"",
    "dev:backend": "cd apps/backend && npm run dev",
    "dev:admin": "cd apps/admin && npm run dev",
    "dev:web": "cd apps/web && npm run dev",
    "dev:rider-app": "cd apps/rider-app && npm start",
    "dev:driver-app": "cd apps/driver-app && npm start",
    "install:all": "npm install && npm run install:apps && npm run install:packages",
    "install:apps": "cd apps/backend && npm install && cd ../admin && npm install && cd ../web && npm install",
    "install:packages": "cd packages/shared && npm install && cd ../ui-components && npm install",
    "build": "npm run build:backend && npm run build:admin && npm run build:web",
    "build:backend": "cd apps/backend && npm run build",
    "build:admin": "cd apps/admin && npm run build",
    "build:web": "cd apps/web && npm run build",
    "test": "echo \"Run tests in individual packages\"",
    "lint": "eslint . --ext .js,.jsx",
    "format": "prettier --write \"**/*.{js,jsx,json,md}\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "docx": "^9.5.1",
    "eslint": "^8.56.0",
    "prettier": "^3.1.1"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
